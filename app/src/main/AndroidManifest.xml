<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2019.ecomerceapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".HGroupApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.Activiteis.AddIM"></activity>
        <activity android:name=".Admin.Activiteis.Add_Item_Image_Activity" />
        <activity android:name=".MapLocation.ShowMapActivity" />
        <activity android:name=".Customers.Activities.Google_Email" />
        <activity android:name=".Customers.Activities.ChatActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Customers.Activities.RegisterByNameAndPhone" />
        <activity android:name=".Customers.Activities.Commdity" />
        <activity android:name=".Customers.Activities.BasktActivity" />
        <activity
            android:name=".Admin.Activiteis.AdminPanel"
            android:label="@string/app_name" />
        <activity android:name=".Admin.Activiteis.AddCategory" />
        <activity android:name=".Base.BaseActivity" />
        <activity
            android:name=".Admin.Activiteis.Commodities"
            android:label="@string/title_activity_commodities"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Admin.Activiteis.Add_Commodity" />
        <activity android:name=".Admin.Activiteis.EditCategory" />
        <activity android:name=".Admin.Activiteis.EditComodity" />
        <activity
            android:name=".Customers.Activities.Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>